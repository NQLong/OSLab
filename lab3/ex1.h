#ifndef ex1
#define ex1

#include <stdio.h>
#include <unistd.h>
#include <signal.h>
#include <sys/types.h>

#define MAX_LEN 80

int divisibleby2(char *str) ;
int divisibleby3(char * str);
int divisibleby5(char *str) ;
int modify(char *str);

#endif